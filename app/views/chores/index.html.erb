<% if user_signed_in? %>
        <%= render 'layouts/sidebar' %>
      <% end %>

<div class="col-med">
    <% if @chores.empty? %>
        <div class="no-chores-text">
            You do not currently have any chores.
        </div>
    <% else %>

        <h2><%= current_user.name %>'s Chores</h2>
        <%= render 'categories/category_header' %>
        <%= render @chores %>
        </table>
    </div>


    <% end %>
</div>