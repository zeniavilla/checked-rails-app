<%= render 'layouts/sidebar' %>

<div class="col-med">
    <h2><%= current_user.name %>'s Chores</h2>
    <% if !@chores || @chores.empty? %>
        <%= render 'shared/add_chore_button' %>
    <% else %>

        <%= render 'categories/category_header' %>
        <%= render @chores %>
        </table>

    <% end %>
</div>